<%= form_for(@lbxid) do |f| %>
  <% if @lbxid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lbxid.errors.count, "error") %> prohibited this lbxid from being saved:</h2>

      <ul>
      <% @lbxid.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :lbxids %><br>
    <%= f.text_area :lbxids %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
